import React, { Component, PropTypes } from 'react';
import ReactDOM from 'react-dom';


export default class RemoveButton extends Component{
  constructor(props){
    super(props);
  }

  render(){
    return(
      // <img src={url} style={this.props.btnStyle} onClick={this.props.closeHandler}></img>
      <div>lkajsd</div>
    )
  }
}
